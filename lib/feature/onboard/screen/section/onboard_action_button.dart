import 'package:flutter/material.dart';

class OnboardActionButton extends StatelessWidget {
  const OnboardActionButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}
