import 'package:flutter/material.dart';

class OnboardIndicator extends StatelessWidget {
  const OnboardIndicator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}
